{
  "name": "matrix-react-native",
  "version": "0.1.0",
  "private": true,
  "packageManager": "yarn@3.2.2",
  "scripts": {
    "init-all": "yarn workspaces foreach run init"
  },
  "workspaces": {
    "packages": [
      "apps/*",
      "libs/*",
      "libs/react-native-cli/templates/*",
      "examples/*",
      "scripts"
    ]
  }
}
